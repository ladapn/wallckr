description: PWM-driven DC motor.

compatible: "pwm-motor"

include: base.yaml

properties:
  pwms:
    required: true
    type: phandle-array
    description: PWM specifier driving the DC motor.
  ph-gpios:
    required: true
    type: phandle-array
    description: GPIO connected to DC motor PH pin.
    
  